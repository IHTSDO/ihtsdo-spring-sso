<?xml version="1.0" encoding="UTF-8"?>
<beans:beans 
	xmlns="http://www.springframework.org/schema/security"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
	xmlns:beans="http://www.springframework.org/schema/beans"
	xsi:schemaLocation="
	http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd
 	http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">

	<!-- REST should have no state, each request is re-authenticated -->
	<http create-session="stateless"
			auto-config="false" 				
			entry-point-ref="http403EntryPoint"
			use-expressions="true"
		>

		<!-- No security on Swagger API site -->
		<intercept-url pattern="/api-docs/**" access="permitAll" />
		<intercept-url pattern="/**/*.html" access="permitAll" />
		<intercept-url pattern="/**/*.js" access="permitAll" />
		<intercept-url pattern="/**/*.css" access="permitAll" />
		<intercept-url pattern="/**/*.png" access="permitAll" />
		<intercept-url pattern="/**/*.jpg" access="permitAll" />

		<!-- Require application role on everything else -->
		<!-- Comment out the line below to disable authentication -->
		<intercept-url pattern="/**" access="hasAnyRole('ROLE_ihtsdo-sca-author')" />
		<http-basic />
		<custom-filter ref="authenticationDecorator" after="BASIC_AUTH_FILTER" />
	</http>

	<global-method-security pre-post-annotations="enabled" secured-annotations="enabled" proxy-target-class="true" />

	<authentication-manager>
		<authentication-provider>
			<user-service>
				<user name="example-user" password="example-password" authorities="ROLE_USER" />
			</user-service>
		</authentication-provider>
	</authentication-manager>

	<beans:bean id="authenticationDecorator" class="org.ihtsdo.example.ims.RequestHeaderAuthenticationDecorator" />
	<beans:bean id="http403EntryPoint" class="org.springframework.security.web.authentication.Http403ForbiddenEntryPoint" />
	
</beans:beans>
